# Runtime deps for `pip install -r` and CI bootstrap.
# Canonical source: pyproject.toml [project.dependencies] (keep both in sync).
# Upstream LMCache is installed from third_party/LMCache by
# scripts/install_dependencies.sh (NO_NATIVE_EXT=1, --no-deps) — do not list
# lmcache here or pip will pull cupy-cuda13x from PyPI metadata.
aiofile
aiofiles
blake3
aiohttp
awscrt
cufile-python
fastapi
httpx
huggingface_hub>=1.5.0
msgspec
# CoreX extensions in the release image are built against NumPy 1.x.
numpy==1.26.4
numba
nvtx
opentelemetry-api >= 1.20.0, <= 1.40.0
opentelemetry-sdk >= 1.20.0
opentelemetry-exporter-otlp >= 1.20.0
opentelemetry-exporter-prometheus >= 0.50b0, <= 0.61b0
prometheus_client >= 0.18.0, <= 0.24.1
psutil
py-cpuinfo
# cupy.testing._random unconditionally imports pytest at module level;
# torch._dynamo triggers this via inspect.getmodule() during compilation.
pytest
pyyaml
pyzmq >= 25.0.0
redis
safetensors
setuptools>=77.0.3,<81.0.0
setuptools_scm>=8
sortedcontainers
uvicorn
httptools
transformers >= 5.4
